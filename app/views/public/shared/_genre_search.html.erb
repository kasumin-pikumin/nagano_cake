     <table class="table table-borderless">
        <thead>
          <th>ジャンル検索</th>
        </thead>
        <tbody>
          <tr>
            <% genres.each do |genre| %>
            <td><%= genre.name %></td>
            <% end %>
          </tr>
        </tbody>
      </table>