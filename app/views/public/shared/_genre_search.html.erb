     <table class="table table-borderless">
        <thead>
          <th>ジャンル検索</th>
        </thead>
        <tbody>

            <% genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
            </tr>
            <% end %>

        </tbody>
      </table>